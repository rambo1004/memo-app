# 메모 앱 작업 내역 (2026년 9월 23일)

## 1. 백엔드 및 데이터베이스 전환 (Supabase -> 로컬 PostgreSQL)
- **작업 내용**: 기존 Supabase 클라이언트 사이드 직접 연결 방식에서 Node.js(Express) + PostgreSQL 로컬 서버 방식으로 구조 변경.
- **주요 파일**: `server.js` 생성, 데이터베이스 접근 권한 보안을 위해 `.env` 도입.
- **네트워크 접근**: 공유기 포트포워딩(3000번)을 설정하여 외부망(`bca.iptime.org:3000`)에서 앱에 접속할 수 있도록 환경 구성.

## 2. 기존 데이터 마이그레이션 및 트러블슈팅
- **작업 내용**: Supabase에 저장되어 있던 기존 사용자들의 일정/할일/메모 데이터를 로컬 PostgreSQL로 옮기는 `migrate.js` 스크립트 작성.
- **트러블슈팅 (42703 에러)**:
  - **원인**: PostgreSQL이 컬럼명을 대소문자 구분 없이 처리하면서 `dataList` 컬럼을 찾지 못하는 문제, 그리고 기존에 잘못 생성되어 있던 테이블 스키마 충돌 문제 발생.
  - **해결책**: 스크립트 내에서 데이터를 옮기기 전 `DROP TABLE IF EXISTS`로 기존에 꼬여있던 테이블을 완전히 삭제한 뒤, 완벽한 스키마(`"dataList"` 대소문자 강제 보존)로 테이블을 재생성하는 로직을 추가하여 이사 완료.

## 3. 일정 알림(알람) 기능 추가
- **작업 내용**: 사용자가 중요한 일정을 등록할 때 원하는 시간에 알람을 설정할 수 있는 기능 추가.
- **기능 명세**:
  - `index.html` 모달의 '일정' 탭에 알람 설정 체크박스와 시간(`HH:MM`) 입력 폼 추가.
  - 백그라운드에서 `setInterval`을 이용해 30초 주기로 현재 시간과 알람이 설정된 일정의 시간을 비교.
  - 조건 일치 시 브라우저 기본 `alert` 팝업과 `Notification` API를 통한 시스템 알림 동시 발생.
  - 리스트 렌더링 시 알람이 설정된 항목에는 `⏰ HH:MM` 뱃지 표시.
